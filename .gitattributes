# 设置默认行为，以防使用不同的行尾处理方式
* text=auto

# 明确声明文本文件应该规范化（转换为平台原生行尾符）
*.py text
*.md text
*.txt text

# 声明文件必须始终使用LF行尾符，即使在Windows上
*.sh text eol=lf

# 声明二进制文件不应该被修改
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
