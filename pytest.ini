[pytest]
markers =
    fast: 快速测试，不需要完整模型加载和执行
    slow: 慢速测试，需要完整模型加载和执行

# 默认运行快速测试，跳过慢测试
addopts = -m "not slow"

# 测试文件匹配模式
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 测试报告设置
console_output_style = progress
